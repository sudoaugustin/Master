const NumDiv=(props)=>(<p key={props.v}onClick={props.onClick} >{props.v}</p>);
function NumPad(props) {
  return(
    <div class="num_pad">
      {
        for (var i = 0; i < 10; i++) {
          array[i]
        }
      }
    </div>
  )

}
